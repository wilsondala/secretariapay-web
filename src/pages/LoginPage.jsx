import { useState } from 'react';import { Navigate,useLocation,useNavigate } from 'react-router-dom';import { ArrowRight,LockKeyhole,ShieldCheck } from 'lucide-react';import useAuth from '../shared/auth/useAuth.js';import { env } from '../config/env.js';
export default function LoginPage(){const {isAuthenticated,login,loading}=useAuth();const location=useLocation();const navigate=useNavigate();const [form,setForm]=useState({email:'admin@secretariapay.com',password:''});const [error,setError]=useState('');const from=location.state?.from?.pathname||'/dashboard';if(isAuthenticated)return <Navigate to={from} replace/>;async function submit(e){e.preventDefault();setError('');try{await login(form);navigate(from,{replace:true})}catch(err){setError(err?.response?.data?.message||err?.response?.data?.error||err?.message||'Não foi possível iniciar sessão.')}}return <div className="min-h-screen bg-imetro-navy"><div className="grid min-h-screen lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden p-10 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute inset-0 bg-[radial-gradient(circle_at_top_left,rgba(215,169,40,.25),transparent_35%),radial-gradient(circle_at_bottom_right,rgba(255,255,255,.12),transparent_35%)]"/><div className="relative"><div className="flex items-center gap-3"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-imetro-gold text-2xl font-black text-imetro-navy">IM</div><div><p className="text-lg font-black">{env.appName}</p><p className="text-sm text-white/65">Painel institucional IMETRO/DCR</p></div></div><div className="mt-28 max-w-2xl"><p className="mb-5 inline-flex rounded-full border border-white/15 bg-white/10 px-4 py-2 text-sm font-bold text-imetro-gold">Gestão automatizada de propinas e atendimento académico</p><h1 className="text-5xl font-black leading-tight">Controlo financeiro académico para o IMETRO.</h1><p className="mt-6 text-lg leading-8 text-white/75">Cobranças, guias, comprovativos, recibos, WhatsApp e validação manual da DCR num único painel.</p></div></div></section><section className="flex items-center justify-center bg-imetro-surface px-4 py-10"><div className="w-full max-w-md"><div className="card p-8"><div className="mb-8 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-imetro-navy text-white"><ShieldCheck size={28}/></div><h2 className="mt-5 text-2xl font-black text-imetro-ink">Entrar no painel</h2><p className="mt-2 text-sm text-slate-500">{env.institutionName} ({env.institutionShortName})</p></div><form className="space-y-5" onSubmit={submit}><div><label className="label">E-mail</label><input className="input" type="email" value={form.email} onChange={e=>setForm(s=>({...s,email:e.target.value}))} required/></div><div><label className="label">Senha</label><div className="relative"><LockKeyhole className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={18}/><input className="input pl-11" type="password" value={form.password} onChange={e=>setForm(s=>({...s,password:e.target.value}))} required/></div></div>{error&&<div className="rounded-2xl border border-red-100 bg-red-50 px-4 py-3 text-sm font-medium text-red-700">{error}</div>}<button className="btn-primary w-full" disabled={loading}>{loading?'Entrando...':'Entrar'}<ArrowRight className="ml-2" size={18}/></button></form></div></div></section></div></div>}
