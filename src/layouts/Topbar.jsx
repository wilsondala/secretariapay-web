import { Bell,Menu } from 'lucide-react';import useAuth from '../shared/auth/useAuth.js';import { env } from '../config/env.js';export default function Topbar({onMenuClick}){const {user,logout}=useAuth();return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/90 backdrop-blur"><div className="flex h-16 items-center gap-4 px-4 sm:px-6 lg:px-8"><button className="rounded-xl p-2 text-slate-600 hover:bg-slate-100 lg:hidden" onClick={onMenuClick}><Menu size={22}/></button><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-imetro-ink">{env.institutionShortName}</p><p className="truncate text-xs text-slate-500">{env.dcrName}</p></div><button className="relative rounded-xl p-2 text-slate-500 hover:bg-slate-100"><Bell size={20}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-imetro-gold"/></button><div className="hidden text-right sm:block"><p className="text-sm font-semibold text-slate-800">{user?.email||'Administrador'}</p><p className="text-xs text-slate-500">{user?.role||'ADMIN'}</p></div><button className="btn-secondary py-2" onClick={logout}>Sair</button></div></header>}
