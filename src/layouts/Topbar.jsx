import { Bell,Menu } from 'lucide-react';import useAuth from '../shared/auth/useAuth.js';import { env } from '../config/env.js';import { appInfo } from '../config/appInfo.js';export default function Topbar({onMenuClick}){const {user,logout}=useAuth();return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/90 backdrop-blur"><div className="flex h-14 items-center gap-3 px-4 sm:px-4 lg:px-6"><button className="rounded-xl p-2 text-slate-600 hover:bg-slate-100 lg:hidden" onClick={onMenuClick}><Menu size={22}/></button><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold text-imetro-ink">{env.institutionShortName}</p><p className="truncate text-xs text-slate-500">{env.dcrName}</p></div><span className="hidden rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-black uppercase text-emerald-700 md:inline-flex">{appInfo.environmentLabel}</span><button className="relative rounded-xl p-2 text-slate-500 hover:bg-slate-100"><Bell size={18}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-imetro-gold"/></button><div className="hidden text-right sm:block"><p className="text-xs font-semibold text-slate-800">{user?.email||'Administrador'}</p><p className="text-[11px] text-slate-500">{user?.role||'ADMIN'}</p></div><button className="btn-secondary py-2" onClick={logout}>Sair</button></div></header>}
