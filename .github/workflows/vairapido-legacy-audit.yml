name: Auditoria completa de domínio legado

on:
  push:
    branches:
      - chore/remove-vairapido-legacy-before-production
  pull_request:
    branches:
      - feat/public-admissions-portal-2026-2027
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Registrar árvore e criar snapshot
        shell: bash
        run: |
          git rev-parse 'HEAD^{tree}' > /tmp/git-tree-sha.txt
          tar --exclude='./.git' -czf /tmp/secretariapay-web-complete-source.tar.gz .
          mv /tmp/git-tree-sha.txt "$GITHUB_WORKSPACE/"
          mv /tmp/secretariapay-web-complete-source.tar.gz "$GITHUB_WORKSPACE/"

      - name: Publicar snapshot
        uses: actions/upload-artifact@v4
        with:
          name: secretariapay-web-complete-source
          path: |
            git-tree-sha.txt
            secretariapay-web-complete-source.tar.gz
          if-no-files-found: error
          retention-days: 1
